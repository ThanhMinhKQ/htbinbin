# Claude Architecture Notes

Claude-facing architecture summaries live here. Human-facing durable docs stay in `docs/`.

## Suggested notes

- `pms.md`: PMS flow map and invariants.
- `ota.md`: OTA ingestion and matching map.
- `inventory.md`: inventory request lifecycle.
- `frontend.md`: template/static conventions.

Do not duplicate full docs; point to `docs/` and key files.
